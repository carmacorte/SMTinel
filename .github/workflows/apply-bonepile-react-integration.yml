name: Apply Bonepile React integration

on:
  push:
    branches: [feature/bonepile-react-integration]
    paths:
      - '.github/workflows/apply-bonepile-react-integration.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch-and-validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Patch More Modules React registry
        shell: python
        run